<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper.ServiceMapper">

	<resultMap id="serviceInfo" type="ServiceInfo">
		<id column="si_code" property="siCode" />
		<result column="service_name" property="serviceName"/>
		<result column="service_jibun" property="serviceJibun" />
		<result column="service_lat" property="serviceLat" />
		<result column="service_lng" property="serviceLng" />
		<result column="service_phone" property="servicePhone" />
		<result column="service_img" property="serviceImg" />
	</resultMap>
	
	<insert id="addInfo" parameterType="serviceInfo">
		INSERT INTO service_info (service_name,service_jibun,service_lat,service_lng,service_phone, service_img)
		VALUES (#{serviceName}, #{serviceJibun}, #{serviceLat}, #{serviceLng}, #{servicePhone}, #{serviceImg})
	</insert>
	<select id="selectService" resultMap="serviceInfo">
		SELELCT * FROM service_info
	</select>
</mapper>
